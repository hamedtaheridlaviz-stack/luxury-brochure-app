# Betterhomes Luxury Brochure Builder with Optional Import

Stable manual brochure generator plus optional Property Finder/Bayut import via Apify.

Required Vercel environment variable:
APIFY_TOKEN = your Apify API token

Manual brochure mode works even when import fails.
